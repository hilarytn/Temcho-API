<!-- views/404.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 Not Found</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f8ff;
            padding: 50px;
        }
        .container {
            text-align: center;
            margin-top: 100px;
        }
        h1 {
            font-size: 100px;
            color: #17a2b8;
            font-weight: bold;
        }
        p {
            font-size: 24px;
            color: #495057;
        }
        .btn-custom {
            font-size: 18px;
            padding: 10px 20px;
            border-radius: 50px;
            text-decoration: none;
            background-color: #007bff;
            color: white;
        }
        .btn-custom:hover {
            background-color: #0056b3;
        }
        .bottle-icon {
            font-size: 80px;
            color: #17a2b8;
            margin-top: 30px;
        }
        .water-drop {
            font-size: 40px;
            color: #007bff;
            animation: drop 1s infinite;
        }
        @keyframes drop {
            0% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(20px);
            }
            100% {
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>

    <div class="container">
        <h1 class="animate__animated animate__fadeIn">404</h1>
        <p class="animate__animated animate__fadeIn animate__delay-1s">Oops! This page is as elusive as the last drop of water in your bottle!</p>
        <div class="bottle-icon animate__animated animate__fadeIn animate__delay-2s">
            💧
        </div>
        <p class="animate__animated animate__fadeIn animate__delay-3s">Looks like we can't find what you're looking for. It's as rare as a dry well around here!</p>
        <a href="/" class="btn-custom animate__animated animate__fadeIn animate__delay-4s">Find Some Refreshment</a>
    </div>

    <!-- Bootstrap JS (optional) and Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

    <!-- jQuery Animation -->
    <script>
        $(document).ready(function() {
            // Custom animations using jQuery
            $("h1").hide().fadeIn(2000); // Fade in the 404 number
            $("p").hide().fadeIn(2500); // Fade in the main message
            $(".bottle-icon").hide().fadeIn(3000); // Fade in the water bottle emoji
            $(".btn-custom").hide().fadeIn(3500); // Fade in the button
        });
    </script>
    
</body>
</html>